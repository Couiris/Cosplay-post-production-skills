# 贡献指南

感谢你改进本项目。请让每项修改保持边界清晰、可验证，并尽量只改变一个主题。

## 提交问题

- 使用对应的 Issue 表单。
- 说明使用的 skill、输入意图、实际输出与期望输出。
- 如需附图，请先移除隐私信息，并确认你拥有分享权限。
- 不要公开 API 密钥、访问令牌、私人照片原件或未授权素材。

## 提交修改

1. Fork 仓库并创建主题分支。
2. 修改对应 skill；入口决策写在 `SKILL.md`，大量条件知识放在 `references/`，可重复的确定性检查放在 `scripts/`。
3. 保持 skill 文件夹名与 frontmatter `name` 一致。
4. 新增 reference 后，必须从 `SKILL.md` 或另一份相关文档链接并说明读取时机。
5. 不提交缓存、生成结果、私人图片、访问令牌或第三方未授权素材。
6. 在仓库根目录运行 `python scripts/validate_skills.py`。
7. 修改任一 skill 时，运行该目录的 `scripts/audit_skill.py` 与 `scripts/test_validator.py`；生成示例预设时再运行对应 preset/pair 校验器。
8. 提交 Pull Request，说明变更范围、理由、验证结果和兼容性影响。

## 内容要求

- 提示词以可观察目标、授权区域、空间关系和验收条件为主，避免只堆叠形容词。
- 不把生成模型的自然语言锁定描述成绝对像素保证。
- 不凭服装臆造不确定的角色设定、标志或文字。
- 参考图不得带入无关人物、水印、签名、品牌或独特构图。
- 保留安全覆盖规则；不通过修图减少身体覆盖。

提交贡献即表示你同意按本项目的 MIT License 授权该贡献。
