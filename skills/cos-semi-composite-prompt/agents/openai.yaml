interface:
  display_name: "COS半合成提示词"
  short_description: "保留场馆墙顶的可验证两阶段半合成"
  default_prompt: "使用 $cos-semi-composite-prompt：先检查人物抠图、显式蒙版、透明补丁、绝对坐标合成和差分能力，再为这张 COS 场照生成 pair_id 一致、保留人物与可见场馆结构、包含清场/分层布景/独立影子/无人无影底板/最终装配层和五道质量门的两阶段 JSON 预设。"
policy:
  allow_implicit_invocation: true
