interface:
  display_name: "COS大合成提示词"
  short_description: "生成完全换景与华丽特效的两阶段COS合成预设"
  default_prompt: "使用 $cos-large-composite-prompt 为这张 COS 照片生成完全换景、华丽特效的大合成预设。"
policy:
  allow_implicit_invocation: true
